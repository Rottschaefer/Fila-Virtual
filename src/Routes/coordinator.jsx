export const goTo = (navigate, path) => {
    navigate(`${path}`);
};

export const goToFila = (navigate) => {
    navigate("/fila");
};

export const goToAdmin = (navigate) => {
    navigate("/admin");
};